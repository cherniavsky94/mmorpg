# Как запустить проект

## Быстрый старт

### 1. Установка зависимостей (если еще не установлены)
```bash
pnpm install
```

### 2. Запуск сервера
```bash
pnpm dev:server
```

Сервер запустится на порту **2567**.

Вы увидите:
```
✅ Colyseus server listening on port 2567
📍 Matchmaking: http://localhost:2567/matchmake/joinOrCreate/city
```

### 3. Запуск клиента (в новом терминале)
```bash
pnpm dev:client
```

Клиент запустится на порту **5173**.

Откройте браузер: [http://localhost:5173](http://localhost:5173)

## Проверка работы сервера

### Health Check
```bash
curl http://localhost:2567/
```

Ответ:
```json
{
  "status": "ok",
  "message": "Colyseus server running",
  "rooms": ["city", "battle"]
}
```

### Matchmaking Endpoint
```bash
curl -X POST http://localhost:2567/matchmake/joinOrCreate/city \
  -H "Content-Type: application/json" \
  -d '{}'
```

Ответ:
```json
{
  "room": {
    "clients": 1,
    "name": "city",
    "roomId": "..."
  },
  "sessionId": "..."
}
```

## Структура проекта

```
mmorpg/
├── client/              # Phaser 3 клиент
│   ├── src/
│   │   ├── main.ts
│   │   ├── scenes/      # Игровые сцены
│   │   ├── network/     # Подключение к серверу
│   │   └── state/       # Zustand store
│   ├── .env             # Для Gitpod (wss://)
│   └── .env.local       # Для локальной разработки (ws://)
│
├── server/              # Colyseus сервер
│   ├── src/
│   │   ├── index.ts     # Точка входа
│   │   ├── rooms/       # Игровые комнаты
│   │   └── store/       # Zustand stores
│   └── .env
│
└── prisma/              # База данных
    ├── schema.prisma
    └── .env
```

## Доступные комнаты

1. **city** - Городская комната (основная)
2. **battle** - Боевая комната

## Переменные окружения

### Client (.env.local для локальной разработки)
```env
VITE_WS_URL=ws://localhost:2567
```

### Client (.env для Gitpod)
```env
VITE_WS_URL=wss://2567--<workspace-id>.gitpod.dev
```

### Server (.env)
```env
PORT=2567
DATABASE_URL=postgresql://...
```

## Команды

### Разработка
- `pnpm dev:server` - Запустить сервер
- `pnpm dev:client` - Запустить клиент
- `pnpm dev` - Запустить оба одновременно (concurrently)

### Сборка
- `pnpm build:server` - Собрать сервер
- `pnpm build:client` - Собрать клиент

### Prisma
- `pnpm prisma:push` - Применить схему к БД
- `pnpm prisma:generate` - Сгенерировать Prisma Client
- `pnpm prisma:studio` - Открыть Prisma Studio

## Troubleshooting

### Порт 2567 занят
```bash
# Найти процесс
lsof -i :2567

# Убить процесс
kill -9 <PID>
```

### Порт 5173 занят
```bash
# Найти процесс
lsof -i :5173

# Убить процесс
kill -9 <PID>
```

### Убить все Node процессы
```bash
killall -9 node
```

### Клиент не подключается к серверу
1. Проверьте, что сервер запущен: `curl http://localhost:2567/`
2. Проверьте `.env.local` в папке client
3. Откройте консоль браузера (F12) для просмотра ошибок

### CORS ошибки
Убедитесь, что в `server/src/index.ts` есть:
```typescript
import cors from "cors";
app.use(cors());
```

## Gitpod

При запуске в Gitpod:
1. Порты 2567 и 5173 автоматически станут публичными
2. Используйте URL вида: `wss://2567--<workspace-id>.gitpod.dev`
3. Обновите `client/.env` с правильным workspace ID

## Что дальше?

1. Откройте [http://localhost:5173](http://localhost:5173) в браузере
2. Вы должны увидеть "Connected to City!" если подключение успешно
3. Откройте консоль браузера (F12) для просмотра логов
4. Начните разработку игровой логики в `client/src/scenes/MainScene.ts`
